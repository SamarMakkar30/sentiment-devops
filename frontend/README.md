# Aethera Hero Frontend

Single-page hero built with React, Vite, Tailwind CSS, and TypeScript.

## Run
```bash
npm install
npm run dev
```

The dev server proxies API calls to http://localhost:5000, so run the Flask
backend on that port in another terminal.

## Build
```bash
npm run build
npm run preview
```
